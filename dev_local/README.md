# Local development stand (test Asterisk + Janus + Caddy + app packages).
#
# From repository root:
#   docker compose -f dev_local/docker-compose.yml up -d --build
#
# Configs in this directory: asterisk/, caddy/, janus/
